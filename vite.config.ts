import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/TreeApp/', // Para GitHub Pages - ajuste o nome do repositório se necessário
})
